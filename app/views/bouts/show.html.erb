  <!-- <div class="medium-15 large-8 columns"> -->
   <article class="stats-center-card">
     <img src="https://s3.amazonaws.com/engardeappfencingimages/banner.jpeg" style="width: 2000px; height: 150px"%></a>

 <div class="card-content">
  <p class="post-author">
    <div class="small-12 columns" id="bout_created">Bout Created: <%= @bout.created_at %>
      </div>
     <% if !@bout.location.nil? %>
       <div class="small-12 columns" id="bout_location">
       Location: <%= @bout.location %>
       </div>
     <% end %>

       <% if !@bout.qualifying.nil? %>
                <div class="small-12 columns" id="bout_qualifying">
                Qualifying: <%= @bout.qualifying %>
                </div>
              <% end %>


  </p>
  </div>
  <p><br></p>
<% if !@bout.notes.nil? %>
  Notes: <%= @bout.notes %>
  <% end %>
<br>
  <% @bout.points.each do | p |%>
          <div>Point: <%= p.status %>, Reason Category: <%= p.reason_category %>, Reason: <%= p.reason %></div>

  <% end %>
     <div class="submenu menu vertical">

     <%= link_to "Add Point",  new_bout_point_path(@bout)%>
     <%= link_to "Back to Fencer", fencer_path(@fencer.id)%>
     <%= link_to "Back to All Fencers", fencers_path%>
     <%= link_to "Performance", fencer_bouts_path(@fencer)%>
     </div>






    </div>


</article>
</div>
</div>
